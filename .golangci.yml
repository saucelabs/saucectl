linters:
  enable:
    - revive
    - errname
    - goimports
linters-settings:
  staticcheck:
    # https://staticcheck.io/docs/configuration/options/#checks
    # Default: ["*"]
    checks: [ "all", "-SA1019" ]
  revive:
    rules:
      - name: redefines-builtin-id
        severity: "off" 
